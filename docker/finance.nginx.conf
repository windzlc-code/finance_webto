server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location = /admin {
        return 302 /admin/;
    }

    location = /admin/ {
        return 302 /admin.html;
    }

    location / {
        try_files $uri $uri/ $uri.html =404;
    }
}
